{
  "name": "video-downloader-backend",
  "version": "1.0.0",
  "description": "Backend API for extracting direct video URLs using yt-dlp (via youtube-dl-exec wrapper).",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "helmet": "^6.0.1",
    "express-rate-limit": "^6.7.0",
    "youtube-dl-exec": "^1.2.4"
  }
}
